import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';

const source = await readFile(new URL('../src/views/Login.vue', import.meta.url), 'utf8');

assert.doesNotMatch(source, /login-bg(?:2)?\.png/);
assert.match(source, /<canvas[^>]+ref="sceneCanvas"/);
assert.match(source, /prefers-reduced-motion/);
assert.match(source, /@submit="login"/);
assert.match(source, /:loading="submitting"/);
assert.match(source, /autocomplete="username"/);
assert.match(source, /autocomplete="current-password"/);
assert.match(source, /100dvh/);
assert.match(source, /@media \(max-width: 860px\)/);
assert.match(source, /theme-quantum/);
assert.match(source, /theme-aurora/);
assert.match(source, /preview-switcher/);
assert.match(source, /get\('preview'\) === '1'/);
assert.match(source, /import\.meta\.env\.DEV && searchParams\.get\('preview'\) === '1'/);
assert.match(source, /ticker-field/);
assert.match(source, /cursor-reticle/);
assert.match(source, /@pointermove="updatePointerEffect"/);
assert.match(source, /scene-effect-switcher/);
assert.match(source, /effectOptions/);
assert.match(source, /get\('effect'\)/);
assert.match(source, /matrix/);
assert.match(source, /gravity/);
assert.match(source, /wave/);
assert.match(source, /previewTheme = ref<PreviewTheme>[\s\S]*?: 'aurora',\n\);/);
assert.match(source, /sceneEffect = ref<SceneEffect>[\s\S]*?: 'matrix',\n\);/);
assert.match(source, /sceneEffect\.value === 'matrix'[\s\S]{0,260}drawMatrixRain\(time\);[\s\S]{0,180}drawThemeSignature\(time\);/);
assert.match(source, /systemCode: 'DNF ADMIN \/ LIVE'/);
assert.match(source, /headline: 'DNF-Admin'/);
assert.match(source, /subtitle: '游戏服务运营管理中心'/);
assert.match(source, /panelKicker: 'GM 后台登录'/);
assert.match(source, /accessLabel: '服务连接正常'/);
assert.match(source, /公会管理/);
assert.match(source, /在线泡点/);
assert.doesNotMatch(source, /AURORA OPS 03/);
assert.doesNotMatch(source, /极光作业台/);
assert.doesNotMatch(source, /实时运维流与权限校验入口/);
assert.doesNotMatch(source, /<span>AUTH<\/span>/);

console.log('login preview structure verified');
